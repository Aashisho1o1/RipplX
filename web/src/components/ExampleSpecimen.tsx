import type { ReactNode } from "react";

const SPECIMEN_LABEL = "Example — not your data";
function SpecimenFrame({ caption, children }: { caption: string; children: ReactNode }) { return <aside className="specimen" data-specimen="true" aria-label={SPECIMEN_LABEL}><p className="specimen-label"><span className="specimen-tag">{SPECIMEN_LABEL}</span>{caption}</p>{children}</aside>; }
export function ExampleFindingSpecimen() { return <SpecimenFrame caption="What a published finding looks like once its quotation has been matched to the filing character for character."><article className="finding"><div className="finding-heading"><h3>Example headline: a qualitative change the model selected</h3><span className="pill neutral">AI-selected · example</span></div><div className="finding-evidence"><div className="evidence"><blockquote className="quote">This placeholder stands in for the exact sentence RipplX copies out of the filing.</blockquote><p className="citation-line"><span className="citation-meta">section key · character offsets · section hash</span><span className="citation faint">Example only — no SEC link</span></p></div></div></article></SpecimenFrame>; }
export function ExampleMetricSpecimen() { return <SpecimenFrame caption="What a verified number looks like: a versioned Python formula over SEC XBRL facts, never a model output."><div className="table-scroll"><table className="metric-table"><caption className="sr-only">Example deterministic SEC XBRL metric result</caption><thead><tr><th>Metric</th><th>Value</th><th>Method &amp; source</th><th>Status</th></tr></thead><tbody><tr><td>Revenue growth</td><td>example value</td><td><code className="formula">revenue_growth.v1</code><span className="metric-source">example computation · computed as of YYYY-MM-DD</span></td><td><span className="trust computed">✓ Computed</span></td></tr></tbody></table></div></SpecimenFrame>; }
