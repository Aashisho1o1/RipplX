# SEC requires a User-Agent header for all EDGAR access.
# finwatch refuses to run any EDGAR command without it.
SEC_USER_AGENT="Your Name your-email@example.com"

# SQLite database location.
FINWATCH_DB=./data/finwatch.db

# One production model/provider for the launch path (litellm format).
# Default: deepseek-v4-flash via OpenRouter (cheap, fast, excellent reasoning).
# To use: set OPENAI_API_BASE=https://openrouter.ai/api/v1 and OPENROUTER_API_KEY.
# Alternative: FINWATCH_MODEL=openai/gpt-4o-mini (OpenAI direct, requires OPENAI_API_KEY).
FINWATCH_MODEL=openai/deepseek-v4-flash
OPENAI_API_BASE=https://openrouter.ai/api/v1

# Provider credentials stay in the environment or may be entered session-only in RipplX.
# For OpenRouter, set OPENROUTER_API_KEY; for OpenAI direct, set OPENAI_API_KEY.
OPENROUTER_API_KEY=
OPENAI_API_KEY=

# Required only when serving on a non-loopback address (the Docker hosted alpha).
# Use a randomly generated value of at least 32 characters and terminate TLS before the container.
FINWATCH_AUTH_TOKEN=
FINWATCH_ALLOWED_HOSTS=your-alpha.example.com
